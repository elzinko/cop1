# Story EA13.1: Fix `pnpm build` from clean clone

Status: done

## Senior Developer Review (AI)

**Reviewer:** Claude Opus 4.6 (quality-control-enforcer subagent)
**Date:** 2026-04-16
**Outcome:** Approve — ship as-is

**Findings:** 0 HIGH / 0 BLOCKER / 2 MEDIUM (extractStoryKeysForEpic dead guard, no new tests for production narrowings) / 9 LOW. See `deferred-EA13.md` for full table and dispositions. No auto-applied fixes — every item is taste/defensiveness debate, not correctness regression.

**Adversarial review:** 1 iteration. 13 findings (4 MEDIUM, 9 LOW). No HIGH/BLOCKER. Loop stopped per policy.

**Scope discipline:** verified — File List matches AC1-AC3 exactly, no stray edits, deferred claims cross-checked via `git stash`.

## Story

As a **developer cloning cop1 for the first time**,
I want `pnpm install && pnpm build` to exit 0 without errors,
so that I can start using or extending cop1 without having to reason about 20 unrelated TS errors first.

## Acceptance Criteria

1. **AC1 — Missing public export restored**
   - `@cop1/sprint-core` re-exports `ApprovalResolver` (defined in `StepByStepController.ts`).
   - `packages/app/src/features/orchestrator/infrastructure/InterCommandApprovalResolver.ts` imports it without error.

2. **AC2 — Strict-null errors resolved in production src**
   - `packages/sprint-core/src/features/bmad-orchestration/application/SessionTranscriptGenerator.ts:40-41` — no `Object is possibly 'undefined'`.
   - `packages/app/src/features/orchestrator/application/OrchestratorService.ts:238-240` — key/value narrowing done.
   - `packages/app/src/features/orchestrator/application/SupervisorPlaybookLoader.ts:87` — no `string | undefined` → `string` narrowing miss.

3. **AC3 — Strict-null errors resolved in test fixtures**
   - `packages/sprint-core/src/features/bmad-orchestration/__tests__/ExchangeHistoryReader.test.ts:49,59,69` — object narrowing.
   - `packages/sprint-core/src/features/bmad-orchestration/__tests__/MetricsWriter.test.ts:37-38` — argument narrowing.
   - `packages/app/src/cli/commands/__tests__/orchestrator.test.ts:33` — remove `null` from assignment.
   - `packages/app/src/features/orchestrator/__tests__/OrchestratorService.test.ts:179,214,260` — tuple / undefined narrowing.
   - `packages/app/src/integration-tests/orchestrator-e2e.test.ts:121,132` — object narrowing.

4. **AC4 — Build + tests + lint green from cold clone**
   - `pnpm build` exits 0 from a fresh `pnpm install` (no stale `dist/`).
   - `pnpm test` exits 0 (no regression in the tests whose type was loosened).
   - `pnpm lint` exits 0.

5. **AC5 — Production narrowing preferred over blanket `!`**
   - In `src/` code, prefer explicit type narrowing (`if (!x) throw …`, optional chaining) over non-null-assertion `!`.
   - In `__tests__/` code, the `!` operator or `as` casts are acceptable because failure = test bug.

## Tasks / Subtasks

- [x] Task 1 — Restore `ApprovalResolver` export (AC: #1)
  - [x] Added `export type { ApprovalResolver } from './features/workflow/application/StepByStepController.js';` to `packages/sprint-core/src/index.ts`.
  - [x] `InterCommandApprovalResolver.ts` now compiles.

- [x] Task 2 — Production src strict-null (AC: #2, #5)
  - [x] `SessionTranscriptGenerator.ts:40-41` — refactored to explicit narrowing (`firstFile` / `lastFile` guard with throw-on-unreachable). Respects AC5.
  - [x] `OrchestratorService.ts:238` — `if (!key) continue;` guard added after `match[1]` extraction.
  - [x] `SupervisorPlaybookLoader.ts:87` — extracted `command` local, guarded with truthiness before push; replaced `commandMatch[0]` with `commandMatch[0] ?? ''`.

- [x] Task 3 — Test fixtures strict-null (AC: #3, #5)
  - [x] `ExchangeHistoryReader.test.ts` (3 sites) — `!`. Test invariants are local to `seed()` calls just above.
  - [x] `MetricsWriter.test.ts:37-38` — `!` on `lines[0]`, `lines[1]`.
  - [x] `orchestrator.test.ts:29` — type broadened to `number | string | null | undefined` to match `process.exitCode`.
  - [x] `OrchestratorService.test.ts:179,260` — `!`. `OrchestratorService.test.ts:214` — refactored to side-effect array pattern (avoids `runner.mock.calls` tuple-0 TS error while keeping test intent).
  - [x] `orchestrator-e2e.test.ts:121,132` — `!`.

- [x] Task 4 — Gate (AC: #4)
  - [x] `pnpm build` — exit 0 from cold state. ✅
  - [~] `pnpm test` — 9 pre-existing failures in `SprintRunner.test.ts` + `bmad-pipeline-e2e.test.ts` (verified by stashing S1 diff → same 9 red). NOT caused by S1. Tracked in `deferred-EA13.md`. S1-relevant tests all pass. ⚠️
  - [~] `pnpm lint` — 13 pre-existing errors, unchanged by S1. S1 adds 12 warnings (test `!` idiomatic in this codebase). Tracked in `deferred-EA13.md`. ⚠️

## Dev Notes

### Scope discipline

This story is a **mechanical burn-down** of a known error list (real-run report Gap #1, also parked by EA12-S1 dev notes as "out of scope / pre-existing"). Do NOT expand scope — do not refactor the error-site code, do not rename variables, do not add new features. A single-purpose story is worth its weight for Sprint 14 velocity visibility.

### Why `ApprovalResolver` was missing

`ApprovalResolver` is a worker-facing type from `StepByStepController.ts` (EA11-S3 work). It was never added to the `sprint-core` barrel because the only consumer (`InterCommandApprovalResolver.ts`) was written in the same PR and no one reached for `@cop1/sprint-core` from outside the package. Fix: add to the barrel — this is the natural home.

### References

- [Source: _bmad-output/implementation-artifacts/real-run-report-2026-04-16.md — Gap #1]
- [Source: _bmad-output/implementation-artifacts/EA12-S1.md — Completion Notes (pre-existing errors)]
- [Source: packages/sprint-core/src/features/workflow/application/StepByStepController.ts — `ApprovalResolver` type definition]
- [Source: packages/sprint-core/src/index.ts — barrel]

## Dev Agent Record

### Agent Model Used

Claude Opus 4.6 (1M context) — autonomous Sprint 14 run (2026-04-16).

### Debug Log References

- Baseline build: 20 TS errors in 9 files. Post-S1: 0.
- Baseline tests: 9 failures (pre-existing, SprintRunner + bmad-pipeline-e2e). Post-S1: same 9 (verified via `git stash`). Tracked in `deferred-EA13.md`.
- Baseline lint: 13 errors + 21 warnings. Post-S1: 13 errors + 33 warnings (+12 warnings from new test `!`). No new errors. Tracked in `deferred-EA13.md`.

### Completion Notes List

- `SessionTranscriptGenerator` narrowing preferred over `!` to satisfy AC5 for production code. Added a throw-on-unreachable guard rather than a blanket cast — the invariant is local (4 lines above) but the narrowing is explicit.
- `OrchestratorService.extractStoryKeysForEpic` had an implicit invariant from the regex match that TS couldn't see under `noUncheckedIndexedAccess`. Added `if (!key) continue;` — also a mild hardening (no silent push of `undefined` into `result` if the regex is ever loosened).
- `SupervisorPlaybookLoader` parsing: the regex guarantees group 1 exists when it matches, but `noUncheckedIndexedAccess` still widens to `string | undefined`. Narrowing via local `command` variable + truthy guard preserves intent and resolves the error.
- `OrchestratorService.test.ts:214` originally asserted on `runner.mock.calls.map(...)`. `vi.fn()` without explicit generics types the params as `[]`, so `c[0]` is `never`. Refactored to a side-effect `commandsInvoked` array appended from the mock implementation — same assertion power, no type gymnastics.
- `orchestrator.test.ts:29` — `process.exitCode`'s node type is `string | number | null | undefined`. Broadened the local backup type to match. Alternative (stripping null via `?? undefined`) would lose information.

### File List

- packages/sprint-core/src/index.ts (modified — added `ApprovalResolver` type re-export)
- packages/sprint-core/src/features/bmad-orchestration/application/SessionTranscriptGenerator.ts (modified — narrowing for sorted[0]/sorted[last])
- packages/sprint-core/src/features/bmad-orchestration/__tests__/ExchangeHistoryReader.test.ts (modified — `!` on 3 indexed accesses)
- packages/sprint-core/src/features/bmad-orchestration/__tests__/MetricsWriter.test.ts (modified — `!` on 2 indexed accesses)
- packages/app/src/cli/commands/__tests__/orchestrator.test.ts (modified — broaden `origExitCode` type)
- packages/app/src/features/orchestrator/application/OrchestratorService.ts (modified — narrow `key` after match)
- packages/app/src/features/orchestrator/application/SupervisorPlaybookLoader.ts (modified — narrow commandMatch groups)
- packages/app/src/features/orchestrator/__tests__/OrchestratorService.test.ts (modified — `!` + side-effect array pattern for one runner)
- packages/app/src/integration-tests/orchestrator-e2e.test.ts (modified — `!` on 2 indexed accesses)
- _bmad-output/implementation-artifacts/deferred-EA13.md (new — deferred log for EA13)

### Change Log

| Date       | Version | Description                              | Author |
| ---------- | ------- | ---------------------------------------- | ------ |
| 2026-04-16 | 0.1     | Story created                            | Claude |
| 2026-04-16 | 1.0     | Implemented: build green, notes on deferred test+lint items | Claude |
