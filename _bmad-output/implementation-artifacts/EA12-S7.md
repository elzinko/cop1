# Story EA12.7: Aspirational features audit — delete/wire burndown, velocity, kpis (A7)

Status: done

## Story

As a **cop1 maintainer**,
I want dead or unwired aspirational feature modules audited and either deleted or wired to real consumers,
so that the codebase reflects actual capabilities and does not carry misleading dead code.

## Acceptance Criteria

1. **AC1 — kpis-dashboard deleted**
   - `kpis-dashboard/` feature module deleted (zero functional consumer — barrel re-export only per SCP §A7).
   - All barrel re-exports referencing it cleaned up.

2. **AC2 — burndown/ decision**
   - `burndown/` module either:
     - (a) Wired to a cop1 command that consumes `/bmad-bmm-sprint-status` output and produces burndown data, OR
     - (b) Deleted with a backlog item for V1.2 if no concrete consumer exists now.
   - Decision recorded in this story's completion notes.

3. **AC3 — velocity-projector/ decision**
   - `velocity-projector/` module either:
     - (a) Wired to a cop1 command, OR
     - (b) Deleted with a backlog item for V1.2.
   - Decision recorded in this story's completion notes.

4. **AC4 — No orphan imports**
   - After deletions, `pnpm build` succeeds with zero import errors.
   - `pnpm test` passes.

## Tasks / Subtasks

- [x] Task 1 — Audit consumers (AC: #1, #2, #3)
  - [x] Grep confirmed: all 3 modules (`kpis-dashboard/`, `burndown/`, `velocity-projector/`) are barrel-only exports with zero external consumers. Modules live in `packages/sprint-core/src/features/` (not `packages/observability/` as the story referenced — story Dev Notes were stale).

- [x] Task 2 — Delete kpis-dashboard (AC: #1)
  - [x] Directory `packages/sprint-core/src/features/kpis-dashboard/` deleted (+ dist).
  - [x] Barrel re-export removed from `packages/sprint-core/src/index.ts`.

- [x] Task 3 — burndown + velocity-projector decision (AC: #2, #3)
  - [x] **Decision**: delete both. V1.2 backlog note recorded below.
  - [x] Rationale: neither has a concrete consumer today. A7 pivot flags them as "pending wire" to `/bmad-bmm-sprint-status`; that wiring is V1.2 scope (depends on BmadCommandStatusAdapter from EA12-S4 follow-up). Keeping dead code inflates the codebase and misleads new contributors.
  - [x] Directories deleted (+ dist); barrel re-exports removed.

- [x] Task 4 — Build + test verification (AC: #4)
  - [x] `pnpm --filter @cop1/sprint-core build` — emits cleanly (only pre-existing unrelated type errors remain).
  - [x] `pnpm vitest run packages/sprint-core packages/app` — 648 passing (down from 659 by the 11 tests that lived in the deleted modules). 9 pre-existing failures unchanged.

## Dev Notes

### Sizing

Small story. The audit and deletion are straightforward. Wiring (if chosen) would bump effort to medium, but deletion is the expected path for most modules given the A6 pivot (no direct BMAD file access means no easy data source for these modules).

### References

- [Source: _bmad-output/planning-artifacts/sprint-change-proposal-2026-04-15-adversarial-review.md — §A7]
- [Source: packages/observability/src/features/burndown/]
- [Source: packages/observability/src/features/velocity-projector/]
- [Source: packages/observability/src/features/kpis-dashboard/]

## Dev Agent Record

### Agent Model Used

Claude Opus 4.6 (1M context) — autonomous Sprint 13 run (2026-04-15).

### Debug Log References

- [Session log](./sessions/EA12-S7-session.md)

### Completion Notes List

- All three modules were pure barrel-only re-exports — zero external consumers (grep-verified). Delete was unambiguous for all three.
- Story Dev Notes referenced `packages/observability/src/features/`; actual location was `packages/sprint-core/src/features/`. Stale reference; noted in session log.
- V1.2 backlog items (to record in sprint-status.yaml or planning docs when V1.2 planning kicks off):
  - **BurndownCalculator reintroduction** — wire to a cop1 CLI command (e.g. `cop1 burndown --sprint N`) that invokes `/bmad-bmm-sprint-status` via `BmadCommandStatusAdapter` (depends on EA12-S4 follow-up) and derives daily snapshots from story-status transitions.
  - **VelocityProjector reintroduction** — same data source; velocity derived from story-done counts per sprint window.
  - `KPIsDashboardService` remains deleted; no reintroduction planned unless a concrete dashboard UI consumer emerges.

### File List

- packages/sprint-core/src/features/kpis-dashboard/ (DELETED — directory + 2 tests)
- packages/sprint-core/src/features/burndown/ (DELETED — directory + 1 test)
- packages/sprint-core/src/features/velocity-projector/ (DELETED — directory + 1 test)
- packages/sprint-core/src/index.ts (modified — 3 barrel re-exports removed; replacement comments reference V1.2 backlog)
- _bmad-output/implementation-artifacts/sessions/EA12-S7-session.md (new)

### Change Log

| Date       | Version | Description                                                             | Author |
| ---------- | ------- | ----------------------------------------------------------------------- | ------ |
| 2026-04-15 | 1.0     | Deleted kpis-dashboard, burndown, velocity-projector (A7 pivot)         | Claude |
