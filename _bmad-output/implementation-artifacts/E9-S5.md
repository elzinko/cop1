# Story 9.5: Rule Approval UI

Status: done

## Story

As a Developer,
I want a Web UI to consult, approve, modify, or reject rule proposals from agents,
so that I can review improvement suggestions and apply them to the project governance.

## Acceptance Criteria

1. Web UI displays a list of all rule proposals from `RuleProposalService.getAll()`, organized by type (architecture, team, agent, quality, process) with status badges (pending/approved/rejected/debated)
2. Each proposal card shows: ruleId, type, description, reason, submittedBy, submittedAt, status
3. Developer can approve (triggers `RuleProposalService.updateStatus(id, 'approved')` + emits `rule.applied` event), reject (with reason text field), or send to debate (triggers `rule.proposal.debated` event)
4. API endpoint `GET /api/rules/proposals` returns all proposals; `PATCH /api/rules/proposals/:id` updates status
5. Real-time updates via SSE — new proposals appear without page refresh

## Tasks / Subtasks

- [x] Task 1: API routes (AC: #4)
  - [x] Add `GET /api/rules/proposals` to HttpServer — calls `RuleProposalService.getAll()`
  - [x] Add `PATCH /api/rules/proposals/:id` with body `{ status, reason? }` — calls `updateStatus()`
  - [x] Emit appropriate events on status change (`rule.applied`, `rule.rejected`, `rule.proposal.debated`)
- [x] Task 2: React UI component (AC: #1, #2, #3)
  - [x] Create `RuleProposalsView` component with type filter tabs
  - [x] Create `ProposalCard` component with status badge, metadata, action buttons
  - [x] Add Approve/Reject/Debate action handlers calling PATCH endpoint
  - [x] Add rejection reason modal/input
- [x] Task 3: SSE integration (AC: #5)
  - [x] Subscribe to `rule.proposal.submitted` and `rule.applied` events in React
  - [x] Auto-refresh proposal list on event received
- [x] Task 4: Tests
  - [x] Unit test API routes with mocked RuleProposalService
  - [x] Unit test React components with mock data

## Dev Notes

### Architecture Patterns

- **HttpServer** (`packages/app/src/features/daemon/infrastructure/HttpServer.ts`): Add routes following existing pattern (GET /health, GET /api/sprint/status). Use `req.params` for `:id`.
- **RuleProposalService** (`packages/sprint-core/src/features/rule-proposal/application/RuleProposalService.ts`): Already has `getAll()`, `getById()`, `updateStatus()` methods. In-memory Map storage.
- **EventBus** (`packages/shared-kernel/src/features/events/domain/EventBus.ts`): Emit via `eventBus.emit(eventType, payload)`. SSE bridge in HttpServer automatically forwards to `/events`.
- **Web app** (`packages/web/src/App.tsx`): Currently has 3 tabs (Projects, Agents, Tasks). Add a 4th tab "Rules" or integrate into existing structure.

### Project Structure Notes

- API routes go in `HttpServer.ts` — no separate router files in current architecture
- React components in `packages/web/src/` — flat structure, no component subdirectories currently
- Follow existing dark theme design system: slate-900 bg, slate-800 cards, blue-400 accent
- Status colors: pending=amber-400, approved=emerald-400, rejected=red-400, debated=blue-400

### References

- [Source: packages/sprint-core/src/features/rule-proposal/application/RuleProposalService.ts]
- [Source: packages/sprint-core/src/features/rule-proposal/domain/RuleProposalTypes.ts]
- [Source: packages/app/src/features/daemon/infrastructure/HttpServer.ts]
- [Source: packages/web/src/App.tsx]
- [Source: _bmad-output/planning-artifacts/epics.md#Epic-9 — E9-S5]

## Dev Agent Record

### Agent Model Used

Claude Opus 4.6

### Debug Log References

No debug issues encountered.

### Completion Notes List

- **Task 1 — API routes:** Added `GET /api/rules/proposals` and `PATCH /api/rules/proposals/:id` routes to HttpServer following inline route pattern. Added `RuleProposalProvider` interface and setter. Enhanced `RuleProposalService.updateStatus()` to emit events: `rule.applied` (approved), `rule.rejected` (rejected), `rule.proposal.debated` (debated). 5 new API tests + 3 new service event tests, all passing.
- **Task 2 — React UI:** Created `RuleProposalsView` component with type filter tabs (all/architecture/team/agent/quality/process) and `ProposalCard` component with status badges, metadata display, and action buttons (Approve/Reject/Debate). Added rejection reason input modal. Integrated as 4th "Rules" tab in App.tsx. Added CSS styles for new badges and components following dark theme design system.
- **Task 3 — SSE integration:** Added EventSource subscription in `RuleProposalsView` for `rule.proposal.submitted`, `rule.applied`, `rule.rejected`, and `rule.proposal.debated` events. Auto-refreshes proposal list on any of these events.
- **Task 4 — Tests:** API route tests done as part of Task 1 (7 tests in HttpServer.test.ts). React component tests: 7 tests for ProposalCard, 7 tests for RuleProposalsView. Service tests: 10 tests in RuleProposalService.test.ts. Added vitest config and testing-library devDependencies to web package.
- **Code Review Fixes (AI):** Fixed 6 issues: H1 (SSE proxy missing in Vite), H2 (PATCH status not validated), H3 (rejection reason silently discarded), M1 (RuleProposalProvider type safety), M2 (no body size limit), M3 (insufficient RuleProposalsView tests). Added `RuleProposalStatus` type, `VALID_RULE_PROPOSAL_STATUSES` constant, `rejectionReason` field to `RuleProposalRecord`, `MAX_BODY_SIZE` limit, status validation in PATCH handler, reason passthrough, and 7 new tests.

### File List

- packages/app/src/features/daemon/infrastructure/HttpServer.ts (modified — added RuleProposalProvider interface, setter, GET/PATCH routes)
- packages/app/src/features/daemon/__tests__/HttpServer.test.ts (modified — 5 new tests for rule proposal API)
- packages/sprint-core/src/features/rule-proposal/application/RuleProposalService.ts (modified — added event emission on updateStatus)
- packages/sprint-core/src/features/rule-proposal/__tests__/RuleProposalService.test.ts (modified — 3 new tests for event emission)
- packages/web/src/App.tsx (modified — added Rules tab and RuleProposalsView integration)
- packages/web/src/ProposalCard.tsx (new — ProposalCard component with status badge, metadata, action buttons)
- packages/web/src/ProposalCard.test.tsx (new — 7 unit tests)
- packages/web/src/RuleProposalsView.tsx (new — RuleProposalsView component with type filter tabs and SSE integration)
- packages/web/src/RuleProposalsView.test.tsx (new — 7 unit tests)
- packages/web/src/index.css (modified — badge styles for approved/rejected/debated, type filter, proposal action styles)
- packages/web/vitest.config.ts (new — jsdom test environment config)
- packages/web/vite.config.ts (modified — added /events proxy for SSE)
- packages/web/package.json (modified — added test devDependencies)
- packages/sprint-core/src/features/rule-proposal/domain/RuleProposalTypes.ts (modified — added RuleProposalStatus type, VALID_RULE_PROPOSAL_STATUSES, rejectionReason field)
- packages/sprint-core/src/index.ts (modified — exported RuleProposalStatus, RuleProposalRecord types, VALID_RULE_PROPOSAL_STATUSES)
- pnpm-lock.yaml (modified — lockfile update from package.json changes)

## Change Log

- 2026-02-25: Implemented Rule Approval UI (E9-S5) — API routes, React components, SSE integration, tests
- 2026-02-25: Code review fixes — Added /events SSE proxy to Vite config (H1), PATCH status validation against valid values (H2), rejection reason passthrough to RuleProposalService (H3), typed RuleProposalProvider interface (M1), body size limit on PATCH handler (M2), added 3 new web tests for filter/approve/SSE (M3), 2 new API tests for invalid status and rejection reason, 2 new service tests for rejection reason
