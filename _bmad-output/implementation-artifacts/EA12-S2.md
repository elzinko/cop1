# Story EA12.2: Recovery E2E test with real AgentSdkSessionAdapter

Status: todo

## Story

As a **cop1 release gatekeeper**,
I want an E2E test that exercises Strategy A (SDK `resume: session_id`) with the real `AgentSdkSessionAdapter` (not `InMemorySessionAdapter`),
so that session recovery is validated before V1.1 "dogfooding unassisted" sign-off.

## Acceptance Criteria

1. **AC1 — Real adapter used**
   - Test instantiates `AgentSdkSessionAdapter` (not `InMemorySessionAdapter`).
   - Session is created, a BMAD command is run, session is suspended, then resumed via `resume: session_id`.

2. **AC2 — Recovery validated**
   - After resume, the session state (context, history, pending operations) is consistent with pre-suspension state.
   - A follow-up BMAD command executes successfully on the resumed session.

3. **AC3 — Crash recovery scenario**
   - Test simulates an abrupt session termination (process kill or adapter disconnect).
   - Recovery path re-instantiates the adapter and resumes from the last checkpoint.

4. **AC4 — CI-runnable**
   - Test runs under `pnpm test:e2e` or equivalent.
   - Uses mocked LLM responses (no real API calls) but real SDK session lifecycle.

5. **AC5 — Migration from Plan B**
   - EA10-S9's `InMemorySessionAdapter` fixture is either replaced or supplemented by this real-adapter test.
   - The sentinel marker left in EA10-S9 for this migration is resolved.

## Tasks / Subtasks

- [ ] Task 1 — Real adapter test harness (AC: #1, #4)
  - [ ] Set up AgentSdkSessionAdapter with mocked LLM backend
  - [ ] Create fixture story for the test

- [ ] Task 2 — Session lifecycle test (AC: #1, #2)
  - [ ] Create session → run command → suspend → resume → run command
  - [ ] Assert state consistency across suspend/resume

- [ ] Task 3 — Crash recovery test (AC: #3)
  - [ ] Simulate abrupt termination
  - [ ] Resume and verify consistency

- [ ] Task 4 — Migrate EA10-S9 sentinel (AC: #5)
  - [ ] Replace or supplement InMemorySessionAdapter fixture

## Dev Notes

### A2 gap — why this matters

EA10-S9 closed V1-light DoD via Plan B (`InMemorySessionAdapter`). Strategy A (`resume: session_id`) was never tested E2E. This is the #1 risk for unassisted dogfooding — if session recovery fails mid-epic, the orchestrator cannot resume. Per SCP 2026-04-15 §A2.

### Sizing note

This is the largest story in Sprint 13. Real SDK session lifecycle testing may surface unexpected behaviors. Budget 1.5× the initial estimate.

### References

- [Source: _bmad-output/implementation-artifacts/EA10-S9.md — Plan B closure, sentinel marker]
- [Source: packages/sprint-core/src/features/bmad-orchestration/infrastructure/AgentSdkSessionAdapter.ts]
- [Source: _bmad-output/planning-artifacts/sprint-change-proposal-2026-04-15-adversarial-review.md — §A2]

## Dev Agent Record

### Agent Model Used

### Debug Log References

### Completion Notes List

### File List

### Change Log
